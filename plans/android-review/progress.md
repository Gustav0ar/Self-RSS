# Android implementation progress

The implementation starts from `cd802e5`, the current `origin/main` fetched on 2026-09-11. Gustavo authorized separate real PRs for review. PRs remain unmerged. Work takes place in `/tmp/rss-android-experience`; the original checkout is preserved.

## Review stack

| Change | Plan | Branch | PR | State |
| --- | --- | --- | --- | --- |
| Roadmap and execution record | 033–049 | docs/android-experience-roadmap | [#46](https://github.com/Gustav0ar/Self-RSS/pull/46) | Open; CI passed after one test rerun |
| Production-aligned fixtures and isolated verification | 033 | test/android-reader-fixtures | [#48](https://github.com/Gustav0ar/Self-RSS/pull/48) | Open; 420 JVM tests, lint, APK builds, real WebView and process/Room checks passed; physical measurements pending |
| Design alternatives and selection | 034 | design/android-reading-experience | [#47](https://github.com/Gustav0ar/Self-RSS/pull/47) | Open; three mocks prepared, selection pending |
| Reader request ownership | 035, first slice | fix/android-reader-requests | [#50](https://github.com/Gustav0ar/Self-RSS/pull/50) | Open; CI green, 432 JVM tests, lint, APK builds and four device checks pass |
| Durable session owner | 035, prerequisite | fix/android-session-owner | [#51](https://github.com/Gustav0ar/Self-RSS/pull/51) | Open; 438 JVM tests, lint, both isolated APK pairs and real encrypted-file/process checks pass |
| Owned storage and historical offline retention | 035, storage prerequisite | fix/android-owned-storage | [#52](https://github.com/Gustav0ar/Self-RSS/pull/52) | Open; 451 JVM tests, lint/minified builds, 27 device tests and actual process recovery pass |
| In-flight cache invalidation | 035, cache prerequisite | fix/android-cache-load-lifecycle | [#53](https://github.com/Gustav0ar/Self-RSS/pull/53) | Open; 460 JVM tests, lint and isolated APK builds pass; independent source review complete |
| Repository account ownership | 035, data integration | fix/android-session-lifecycle | [#54](https://github.com/Gustav0ar/Self-RSS/pull/54) | Open; 481 JVM tests, lint, isolated builds, 37 emulator checks and external process recovery pass |
| Authentication action ownership | 035, authentication | fix/android-auth-actions | [#55](https://github.com/Gustav0ar/Self-RSS/pull/55) | Open; 490 JVM tests, lint, isolated builds and 17 emulator checks pass; source review complete |
| Authenticated result ownership and offline admission | 035, authentication contract | fix/android-authenticated-owner | [#56](https://github.com/Gustav0ar/Self-RSS/pull/56) | Open; Android CI green, 497 JVM tests, lint, isolated builds and 18 emulator checks pass |
| Account screen lifetimes | 035, feature ownership | fix/android-account-screens | [#57](https://github.com/Gustav0ar/Self-RSS/pull/57) | Open; Android CI green, 500 JVM tests, 21 emulator checks, lint and isolated APK builds pass |
| Foreground collection and cached subscription reads | 035, 036 prerequisite | fix/android-foreground-work | [#58](https://github.com/Gustav0ar/Self-RSS/pull/58) | Open; Android CI green, 511 JVM tests, 26 emulator checks, lint and isolated APK builds pass |
| Worker execution ownership | 035, remaining | fix/android-worker-account-lifecycle | [#59](https://github.com/Gustav0ar/Self-RSS/pull/59) | Open; Android CI green, 513 JVM tests, lint, isolated builds and actual process/startup recovery pass |
| Cache-first actions and bounded outbox delivery | 036, delivery | fix/android-outbox-delivery | [#60](https://github.com/Gustav0ar/Self-RSS/pull/60) | Open; Android CI green at `0187f88`; 518 JVM tests, isolated builds, lint and seven emulator checks pass |
| Revision-ordered local article state | 036, reconciliation | fix/android-mutation-reconciliation | [#62](https://github.com/Gustav0ar/Self-RSS/pull/62) | Open; Android CI green at `6fe1f8f`, 538 JVM tests, lint, isolated builds and 30 emulator checks pass |
| UI mutation ordering and durable unread counts | 036, remaining | fix/android-article-actions | [#63](https://github.com/Gustav0ar/Self-RSS/pull/63) | Open at `f452287`; Android CI green in run `34689697049`; prior native fullscreen crash remains unexplained; 566 JVM tests, lint, isolated builds and 111 API 35 device checks pass. Bulk state catch-up and actual process-death mutation recovery remain |
| Revisioned article snapshots | 036, API prerequisite | feat/article-state-snapshots | [#61](https://github.com/Gustav0ar/Self-RSS/pull/61) | Open; repository CI and Android CI green at `3c22340`; SQLite supplies paired state on cache hits; API, web and Android validation pass |
| Bounded article state lookup | 036, bulk prerequisite | feat/article-state-lookup | [#64](https://github.com/Gustav0ar/Self-RSS/pull/64) | Open at `14c8bd4`; Android and repository CI green in runs `34689742922`/`34690744781`; 143 API integration, 734 API unit and 567 Android JVM tests, types, lint and isolated builds pass |
| Observable local article state and bounded refresh | 036, UI prerequisite | fix/android-observed-article-state | [#65](https://github.com/Gustav0ar/Self-RSS/pull/65) | Open at `1fc74a2`; Android CI green in run `34691293659`, including 116 device tests; 577 JVM tests, lint and isolated builds pass |
| Room-driven article presentation | 036, UI integration | fix/android-article-state-presentation | [#66](https://github.com/Gustav0ar/Self-RSS/pull/66) | Open at `5ca7ed1`; 582 JVM tests, lint and isolated builds pass; all 117 API 35 device cases passed before the final scheduler boundary fix, then all 34 affected device cases passed |
| Outbox recovery after real process death | 036, acceptance | test/android-outbox-process-recovery | pending | Ready for PR; both real process-recovery cases and performance-target lint pass |
| Main-safe I/O | 037 | perf/android-main-safe-io | pending | Reconcile existing PR #40 |
| Loading and error lifecycle | 038 | fix/android-loading-lifecycle | pending | Pending |
| Media and renderer lifetime | 039 | fix/android-reader-media-lifecycle | [#49](https://github.com/Gustav0ar/Self-RSS/pull/49) | Open; CI green, 423 JVM tests, lint/minified build and nine device tests passed |
| Reader readiness and fallback | 040 | fix/android-reader-readiness | pending | Pending |
| Retention metadata and budgets | 041 | perf/android-cache-retention | pending | Pending |
| Durable offline preparation and resource resolver | 042 | feat/android-durable-offline | pending | Pending |
| Reading context restoration | 043 | fix/android-reading-context | pending | Reconcile existing PR #45 |
| Idle and long-article rendering | 044 | perf/android-long-article-reader | pending | Pending |
| Queue and Search UX | 045 | feat/android-queue-search | pending | Reconcile existing PR #43 |
| Adaptive reader and finite motion | 046 | feat/android-adaptive-reader | pending | Pending design selection |
| Drafts, settings and management | 047 | fix/android-editing-resilience | pending | Reconcile existing PRs #39 and #44 |
| Realtime gap recovery | 048 | fix/android-realtime-recovery | pending | Pending |
| Integrated acceptance evidence | 049 | test/android-experience-acceptance | pending | Pending |

Dependencies will use stacked PRs when required. Each PR names its base and the checks for its own change. Source already corrected on main will receive verification and remaining fixes, not a duplicate implementation.

## Environment and evidence

- GitHub authentication works with network access; no open PR existed at reconciliation.
- Isolated SDK/build tools and Gradle are working. The dedicated `small_phone` API 36.1 emulator is booted at `emulator-5566`; normal app packages remain untouched.
- Rich WebView readiness and basic Room/task recovery after actual background process death pass on the dedicated emulator. All 420 JVM tests, lint, both APK pairs and final fixture checks pass. Physical performance and native/WebView memory evidence remain pending.
- `html-communication` is absent. Three local design alternatives and screenshots are in PR #47; Gustavo’s selection and optional private Sites publication reply are pending.
- No production service, user database, daily-driver app, or release channel has been changed.

## Completion rules

- Keep code completion, unit/build results, device results, PR checks, and design selection separate.
- Preserve supported Room histories, including both version-6 layouts. Export/version every persistent schema change and test clean creation plus upgrades.
- Lifecycle tests must cover actual active-page/foreground transitions. Process-death tests need a runner outside the target app.
- Measure release-equivalent performance on identified hardware. A passing build or stable JVM heap does not prove WebView/native memory behavior.
- Final acceptance remains incomplete until all required evidence exists. No claim of zero leaks without the bounded repeated-open/close measurements.

## Execution log

- Reconciled the original review at `74d5c11` with main `cd802e5`. Eight Android PRs landed in between and are mapped above.
- Preserved the original checkout and created an isolated worktree.

- PR #48 Android CI passed all three required jobs. Copilot could not review because its requester quota was exhausted; the independent source review completed and both findings were fixed.
- Plan 039 verified real audio/video pause, fullscreen restoration, renderer-loss recovery after enrichment, bounded views, trim and close on WebView 134.0.6998.135. Physical memory evidence remains pending.

- PR #49 Android instrumentation, JVM tests and lint/build passed. Copilot's quota still prevented its review; independent review findings were reproduced and resolved locally.
- Plan 035 is split into a reader-request PR and the remaining account/foreground work. Two review findings were reproduced and fixed, including a warming cancellation crash. Account transitions need durable queue ownership before replacing the current destructive clear.
- Reader-request slice: `/tmp/android-session-reader-device-final.log` passes Back with delayed detail/read responses, opening another article, latest resume callback and disposal, plus both fast-swipe cases. Tested on dedicated API 36.1 emulator, WebView 134.0.6998.135. `/tmp/android-session-reader-lint-isolated.log` passes lint in a fresh worker after a reused Kotlin lint process crashed; no check was disabled.
- Durable-owner prerequisite: `/tmp/android-session-owner-final-build.log` passes 438 JVM tests and both isolated APK pairs. `/tmp/android-session-owner-lint-final.log`, `/tmp/android-session-owner-device-final.log` and `/tmp/android-session-owner-process-final.log` pass. The independent review's pre-preload authority defect was reproduced in `/tmp/android-session-owner-review-red.log` and fixed; a controlled preload/logout overlap also passes.
- PR #50 completed all Android CI checks on `3a2b070`. Copilot quota remains exhausted; the independent review completed.
- PR #51's first CI run passed lint/build and the new session migrations, but exposed two older test synchronization gaps: checking Room Paging invalidation before its executor callback and injecting a media gesture before Chromium visual readiness. The repository assertion now awaits invalidation; media controls wait for drawing and report actual click/promise outcomes. All 438 JVM tests, lint and eight selected device tests pass in `/tmp/android-session-owner-ci-{build,lint,device-final}.log`; renewed CI remains pending.
- The corrected JVM check passes CI. The next CI emulator run crashed during fullscreen entry and retained no crash diagnostics. The instrumentation job now captures emulator logcat on failure and uploads its test reports. This failure remains unresolved; local success does not replace that evidence.
- PR #51 diagnostic run `34673036348` passed all Android jobs. The next revision also makes media fixture controls fit a narrow reader; 320dp device playback/fullscreen checks pass. The previous crash had no retained stack, so its exact cause remains unknown. The latest revision is still running CI.
- Room 8 preserves existing rows and adds a durable active owner, inactive archived queues and legacy offline pins. Direct 6 → 8 preserves the historical pin table before the unchanged 6 → 7 repair drops it; 7 → 8 adds tables without rewriting existing rows. Both version-6 layouts and every other supported upgrade are verified with populated data on JVM and Android SQLite.
- Storage review reproduced two malformed-body failures, in saved-list and bulk-read reconciliation. Both now preserve raw data while applying metadata updates. Logs: `/tmp/android-owned-storage-{red,review-red,bulk-review-red,verification,device,lint-build}.log`. All 451 JVM tests and 27 device tests pass; the minified build and lint pass. Repository request/commit integration is still pending, so this is not yet an account-isolation completion claim.
- The external process harness also passes on the minified Room 8 build in `/tmp/android-owned-storage-process.log`, retaining its synthetic article, durable session owner and task state after confirmed process death. This does not yet exercise production account transitions across the two stores.
- Cache prerequisite: stale loads after clear, namespace invalidation and newer writes were reproduced, along with an admission race. Atomic registration/publication and reference-counted per-key coordination now pass nine added cases. All 460 JVM tests, lint and isolated APK builds pass in `/tmp/android-cache-load-{build,lint}.log`. Independent review confirmed ticket cleanup and prompted separate gauges for registered keys versus active callers. This fixes cache bookkeeping, not the still-pending repository/account boundary.

- Latest Android CI is green for PRs #51 (`a403d8c`), #52 (`fdf7f48`) and #53 (`6fd03a9`), including instrumentation, unit tests and lint/build. Account boundary primitives pass seven controlled lifecycle/recovery cases; repository integration remains in progress.

- Repository ownership is integrated through one account boundary. Independent review findings around queue adoption, non-cancellable local handoff, structured children, buffered notifications, old analytics metadata and registration callbacks were reproduced and corrected. All 481 JVM tests, lint, both isolated APK pairs and 37 emulator checks pass. The external process harness also passes using the production server-switch path; its previous direct SessionStore fixture setup bypassed the new ownership boundary and was corrected. Logs: `/tmp/android-account-final-{build,lint,device}.log`, `/tmp/android-account-process-boundary.log`. Plan 035 remains in progress for queued UI actions, account-specific ViewModel lifetime and foreground work.

- Authentication command ownership: six delayed-command races and a bootstrap recreation failure were reproduced, then fixed. Review also reproduced registration metadata being lost after a harmless error dismissal; only the owned field is now updated. All 490 JVM tests, both isolated APK pairs and 17 real authentication/Activity checks pass. The form fixture's previously off-screen click now scrolls and checks visibility first. Final lint passed in `/tmp/android-auth-actions-final-lint.log`; account feature stores and foreground work remain pending.

- PR #54 (`b23e229`) passed all Android CI jobs in run `34676629308`. PR #55 (`a271989`) also passed all Android CI jobs in run `34677163365`.
- The authenticated-result contract now returns its immutable owner with verified user data or validated offline access. Offline admission stays within the failed restore's original owner, does not renew its lease, and uses the durable owner ID as the reading restore namespace. Review reproduced a failed lease timestamp write and a swallowed analytics auth rejection. Lease-write I/O failure now retains an existing valid lease, while analytics rejection executes the normal authenticated account-clear path. Incompatible verified user data remains an error. Focused tests pass; full build/lint/device verification is in progress.

- Authenticated owner validation: all 497 JVM tests and both isolated APK pairs pass in `/tmp/android-authenticated-owner-reviewed-build.log`. All 18 authentication/Activity device checks pass in `/tmp/android-authenticated-owner-device.log`, including retaining the open article and authenticated model through actual Activity recreation. Final lint passed in `/tmp/android-authenticated-owner-lint.log`. The external process fixture was not rerun for this slice because it does not exercise this authentication UI; full production process restoration remains plan 043/049 work.

- PR #56 (`ceeeffd`) passed all Android CI jobs in run `34677975144`.
- Account feature repositories now receive immutable ownership through Hilt's ViewModel component. Retired screens cannot admit replacement-account requests, synchronous cache reads or cold event flows. Navigation3 scopes the four feature models to the account entry. The first three ownership cases failed before the fix. Independent review also identified a stopped-Activity sign-out/recreation retention gap; the real device test reproduced it in `/tmp/android-account-screens-stopped-red.log`. The host now retains the last entry membership processed by Navigation3 and reconciles it on reattachment. This preserves same-account recreation and removes departed stores and their jobs. Final verification is in progress; this is disposal evidence, not a native heap leak measurement.

- Account screen verification passed: 500 JVM tests and isolated APK pairs in `/tmp/android-account-screens-final-build.log`; 21 emulator checks in `/tmp/android-account-screens-device-final.log`; final lint and the minified build in `/tmp/android-account-screens-final-lint.log`. The only change after the device run replaces deprecated `bundleOf` with equivalent typed `Bundle.putString`; compilation/lint/minification pass. Foreground work and full production process restoration remain separate slices.

- PR #57 (`90a6d2f`) passed every Android CI job in run `34679287338`.
- Foreground reads and SSE now stop at Activity STOP through structured lifecycle children. User queue submission remains independent. Review reproduced cold-offline hydration, slow submission/status ordering and pending-count regressions; all were fixed. Stored subscription streams are explicit about count authority and keep network freshness within the collector. All 511 JVM tests and isolated builds pass in `/tmp/android-foreground-final-build.log`; all 26 integrated emulator checks pass in `/tmp/android-foreground-device-final.log`. The blocked-request fixture now waits for Compose foreground activation before awaiting its external request gates. Source review is complete; final lint is in progress. Worker lifetime and durable counter reconciliation remain pending.

- Final foreground lint passed in `/tmp/android-foreground-final-lint.log`. Repeated STOP/START cancels blocked health/status calls and SSE, restores one subscription, retains user queue submission and restores the cold offline drawer. Account/authentication/recreation checks remain green in the same 26-test device run.

- PR #58 (`e4ad9bb`) passed every Android CI job in run `34680812324`.
- Worker execution now retains its original account across authentication admission, queue submission, delays and status polling. The old worker used the replacement account on its second poll in `/tmp/android-worker-owner-red.log`; that case and ordinary blocked-poll cancellation now pass. Startup uses the shared application scope and preserves existing unique work with KEEP. A real WorkManager test verifies repeated scheduling retains the original pending request IDs.
- All 513 JVM tests and both isolated APK pairs pass in `/tmp/android-worker-final-build-retry.log`; lint passes in `/tmp/android-worker-final-lint.log`. The first full suite encountered a host JBR C2 compiler SIGSEGV in `Node::uncast`, retained in `/tmp/android-worker-jbr-crash.log`. A fresh Gradle process passed without changing tests or disabling checks. Independent source review is complete. Outbox delivery scheduling and confirmed rejection handling remain plan 036 work.

- Production Application startup and Room/session recovery after actual background process death pass in `/tmp/android-worker-process.log` on the dedicated API 36.1 emulator. This exercises the injected application scope on the minified package; full production reader restoration remains plan 043. Plan 035 implementation is complete, with combined acceptance and physical memory evidence still tracked by plan 049.

- PR #59 (`25f7a0e`) passed every Android CI job in run `34681457192`.

- Cached subscriptions and read/save actions no longer wait for mutation transport. Durable WorkManager scheduling coalesces queued work, retains a running attempt and one successor, and resets completed history. Review reproduced a manual-retry account-lock wait and accumulating successful work history; both are fixed. Explicit outbox delivery now routes confirmed authentication rejection through normal owner clearing and queue archival.
- All 518 JVM tests and both isolated APK pairs pass in `/tmp/android-outbox-isolated-build.log`. Seven emulator checks pass in `/tmp/android-outbox-device.log`, including real Android WorkManager startup/burst/completion contracts and foreground lifecycle checks. The combined suite exposed a closed WorkManager test delegate leaking between fixtures; cleanup now resets the pinned testing hook after closing Room. Final lint passes in `/tmp/android-outbox-final-lint.log`. Effective mutation/count reconciliation and actual process-death mutation recovery remain plan 036 work.

- PR #60's first CI run passed device/lint/build but timed out waiting for an authentication-loss event. Source review traced a Room/coroutine cancellation handoff; an inline continuation reproduces the failure in `/tmp/android-auth-loss-publication-red.log`. Credential clearing and auth-loss publication now complete in the same short non-cancellable block, with ordinary cancellation preserved afterward. All 518 JVM tests, lint and both isolated APK pairs pass in `/tmp/android-outbox-ci-fix-verification.log`. Renewed CI is pending.


- Article snapshot prerequisite: list/detail/search responses now include existing state revisions, including HTTP validators when flags toggle back. Redis remains a content cache; a bounded SQLite read supplies current flags, revisions and ownership on each hit. Source review found stale warmer, bulk-detail and mixed-version forwarding cases. The implementation removes cached state patches/indexes and omits revisions from stored payloads so old API processes cannot advertise mismatched pairs. Missing owned rows produce detail 404 or a normal list query.
- Snapshot SQL reproduction: `/tmp/article-state-snapshots-red.log`; mixed-writer reproduction: `/tmp/article-state-mixed-writer-red.log`. Final integration passes all 138 cases in `/tmp/article-state-authority-final-integration.log`, including actual SQLite/Redis/HTTP, stale writers, legacy bodies, bulk changes, foreign/deleted rows and ETags. All 734 API unit cases passed before the final payload omission; the 32 affected service/cache cases passed again afterward. All 403 web unit tests and its Node production build pass. Final repository lint/architecture and all package typechecks pass. Test artifact files use the `/tmp/article-state-authority-` prefix.
- Generic repository lint previously attempted to format generated Room schema 8 and validate intentionally malformed publisher HTML. Biome now excludes those two data directories, as it already excludes generated Drizzle/OpenAPI data. Android migration and fixture validation remain required. The API change adds one small indexed SQLite read per cache hit; it does not claim measured Android rendering or native heap performance.

- PR #60 passed every Android CI job at `0187f88` in run `34683804889`. PR #61 passed repository CI in `34684215802` and Android CI in `34684216168`, both at `3c22340`.
- Android confirmed-state reconciliation is in progress. Room 9 preserves old revisions and cached bytes. Review found schema 8 could retain a revision beside a newer unversioned flag, so migration leaves those confirmed values unknown until an equal/newer versioned response arrives. Page/search/body merges, bulk receipts, and unknown rejection are under focused test. This is not yet a completed PR or final performance verification.

- PR #62 passed every Android CI job at `6fe1f8f` in run `34685675004`.
- Action/count follow-up remains unpublished. The counter race was reproduced in `/tmp/android-action-count-publication-red.log`. Counts now change inside the outbox transaction, retain signed values internally, and carry the originally adjusted count scope with each queued read. This covers zero-count rollback, metadata introduced during delivery, search-only membership and nested categories. Same-kind remote reads serialize; durable epoch/pending admission protects Room and foreground count refreshes coalesce without a stats-write feedback loop. Event-based count arithmetic and its obsolete authority wrapper were removed.
- `/tmp/android-count-repository-integration.log` passes the focused repository, account, count, migration and feed/settings suites. `/tmp/android-late-reader-save-red.log` reproduces a completed bookmark being overwritten by delayed reader content. Its shared publication fix and mutation-ID rejection gates are undergoing focused testing. Full JVM/lint/device validation has not yet run for this follow-up.

- Action/count validation passes all 566 JVM tests, lint and both isolated APK pairs in `/tmp/android-actions-counts-final-build-2.log`. All 51 selected device checks pass in `/tmp/android-actions-counts-device.log`, including real Room count/reopen contracts, every supported migration, ownership, read synchronization, foreground lifecycle and fast swipes. The first device APK build rejected shared test names containing spaces; those methods now use DEX-compatible names. No production build settings or checks were relaxed. Schemas 8 and 9 remain unchanged. Independent source review found no new blockers; remaining flag authority, bulk catch-up and actual outbox process-death work stay open.

- PR #63 CI run `34688855223` passed JVM and lint/build but crashed during fullscreen video backgrounding after 56 device tests passed. Retained logcat identifies a null dereference in WebView 124.0.6367.219 `VizWebView`, inside `libwebviewchromium.so`, on API 35. An isolated API 35 emulator with that exact provider passes all six media tests, then passes ten repeated fullscreen/background cycles, then all 111 device tests with the repeated case. The native failure is not reproduced locally and its cause remains unconfirmed. The repeated case now runs in CI; no production workaround or provider upgrade hides it. Evidence: `/tmp/android-actions-ci-artifacts/`, `/tmp/android-api35-media-reproduction.log`, `/tmp/android-api35-media-repeat-red.log` and `/tmp/android-api35-full-repeat-red.log`. The latter two names describe attempted reproductions; both passed.

- Bounded state lookup passes real HTTP/auth/SQLite/Redis checks in `/tmp/article-state-lookup-integration.log`: all 143 API integration cases. All 734 API unit tests pass in `/tmp/article-state-lookup-unit.log`; all package typechecks, repository lint/architecture, generated OpenAPI and Android contract mapping pass. All 567 Android JVM tests, lint and both isolated APK pairs pass in `/tmp/article-state-lookup-android.log`. The new Moshi check rejects absent revision metadata. The first OpenAPI placement exceeded the existing file budget; its path now lives in the article-state fragment. Independent review found no blockers.

- PRs #63 and #64 passed Android instrumentation, JVM tests, lint and builds on `f452287` and `14c8bd4`. The repeated fullscreen test passed on CI. This does not resolve the earlier WebView 124 native SIGSEGV; its cause remains unknown.

- Observable article-state foundation preserves logical action identity across transport rebases and returns matching rejection identity. Room emits one owner-filtered transaction for the requested IDs, with no reader-body decoding in observation. Network batches validate the entire returned partition before writes, retain missing offline content and cancel on account replacement. Validation: `/tmp/android-observed-state-{red,focused,build,device}.log`; 577 JVM and 36 device tests pass, lint/builds pass, schemas 8–10 unchanged. UI wiring remains a separate slice.
- Repository CI for PR #64 was manually dispatched as run `34690744781`; the workflow only auto-runs for PRs targeting main/master, so its stacked base did not trigger it.

- PR #65 CI run `34690881738` passed 115/116 device cases and failed the repeated fullscreen test before entry. The failure was a gesture-completion `ComposeTimeoutException`, with no native SIGSEGV. Its helper caught `Exception` but Compose timeout extends `AssertionError`, so the click/promise diagnostics were omitted. The helper now preserves those diagnostics for that failure too. A new CI attempt is diagnostic, not a claimed behavior fix. Artifacts: `/tmp/android-observation-ci-artifacts`, `/tmp/android-observation-ci-failure.log`.

- PR #64 repository CI passes at `14c8bd4` in run `34690744781`. PR #65 Android CI passes at `1fc74a2` in run `34691293659`, including all 116 device cases. The diagnostic fixture change does not establish the cause of either earlier fullscreen failure.

- Room-driven presentation removes boolean event authority and uses bounded observed state with durable action receipts. Reproductions cover stale publication, queue failures, unknown rejection, observation lifecycle, superseding choices and legacy-event catch-up. Optional image prefetch and WorkManager scheduling now run in the existing application scope after durable intent commits; controlled tests first reproduced both operations incorrectly delaying or rejecting an accepted edit. Final validation: 582 JVM tests, lint and both isolated APK pairs in `/tmp/android-state-presentation-verified-build.log`; all 34 affected API 35 device checks in `/tmp/android-state-presentation-verified-device.log`. The earlier full device run passes all 117 cases in `/tmp/android-state-presentation-device-3.log`. Independent source review found no new blockers.
- Two local JVM runs stalled while waiting inside coroutine tests. Owned test-worker thread dumps are retained in `/tmp/android-state-test-threads.txt` and `/tmp/android-presentation-final-test-threads.txt`. Subsequent controlled scheduling tests established the receipt-wait defect, but do not establish that defect as the cause of the original stalls. The final complete suite passes without increasing timeouts or disabling checks. Actual queued mutation process-death recovery remains open.

- Plan 036 scoped acceptance is complete. `/tmp/android-outbox-process-final.log` passes both external process cases on API 35, including actual background kill, exact cached HTML/text, unchanged session owner and mutation IDs, lost read acknowledgment retry and automatic WorkManager delivery to empty queues with revision 1/1. Read-only review corrected fixture identity/readiness, body assertions and test-only teardown. `/tmp/android-outbox-process-final-lint.log` passes performance-app lint. These checks use a loopback server in the separate runner and change no production code/schema; full-app physical measurements remain pending.
