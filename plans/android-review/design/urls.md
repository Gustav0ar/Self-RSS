# Android mock review URLs

These are checked local previews, not hosted publications. The dedicated preview serves only synthetic mock files. Hosting through Sites is awaiting approval because the requested html-communication skill is unavailable.

- Queue first: http://127.0.0.1:46381/dense.html
- Reading first: http://127.0.0.1:46381/editorial.html
- Library and reader: http://127.0.0.1:46381/adaptive.html

All three passed browser interaction and phone/tablet overflow checks. Settled screenshots are in screenshots/. These URLs require the local review server; the static files can also be served from this directory after checkout.

The large-text toggle doubles the base text size. No-motion disables finite CSS reveals. Native Android behavior requires separate app testing.
