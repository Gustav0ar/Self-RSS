Synthetic reader corpus. No publisher content or user data. Short/long text, summary, wide table, malformed HTML, image and one-second looping local media are shared by androidTest and performanceTest. The 200-section long fixture is approximately 175 KB. Resolve relative media against the same local asset origin. tone.wav is a mono 16 kHz 440 Hz sine at low amplitude; motion.mp4 is a generated 160x90 test pattern. These assets establish repeatable inputs; each lifecycle/offline plan adds assertions through the production renderer and resource resolver. Normal debug/release assets exclude this directory.
