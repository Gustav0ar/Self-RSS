<?xml version="1.0" encoding="utf-8"?>
<!--
    Debug-only override of the production network security config that allows
    cleartext to the local API host (10.0.2.2 = host machine from emulator,
    localhost = host machine from some other runtimes like Waydroid).
    Stripped down to a domain-config so the production <base-config> "no
    cleartext" rule still applies to every other host.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>
</network-security-config>
